// backend/src/constants/attendanceStatus.js

const ATTENDANCE_STATUS = Object.freeze({
  PRESENT: 'present',
  ABSENT: 'absent',
  LEAVE: 'leave',
  HALF_DAY: 'half-day'
});

export default ATTENDANCE_STATUS;
